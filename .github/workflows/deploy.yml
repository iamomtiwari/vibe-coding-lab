name: Deploy Todo App to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - '01-todo-app/**'
      - '.github/workflows/deploy.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: 01-todo-app/package-lock.json

      - name: Install dependencies
        working-directory: 01-todo-app
        run: npm ci

      - name: Run tests
        working-directory: 01-todo-app
        run: npm test

      - name: Build
        working-directory: 01-todo-app
        run: npm run build
        env:
          GITHUB_PAGES: true

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: 01-todo-app/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
